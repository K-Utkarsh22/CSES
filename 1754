#include <bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	cin>>t;
	while(t--)
	{
		long long a,b,x,y;
		cin>>a>>b;
		if((2*a-b)%3==0 && (2*b-a)%3==0)
		{
			x=(2*a-b)/3;
			y=(2*b-a)/3;
			if(x>=0 && y>=0) cout<<"YES\n";
			else cout<<"NO\n";
		}
		else cout<<"NO\n";
		
	}
}
