#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,q;cin>>n>>q;
    vector<int>p(n+1);
    for(int i=1;i<=n;i++)
    {
        int x;
        cin>>x;
        p[i]=p[i-1]^x;
    }
    while(q--)
    {
        int a,b;
        cin>>a>>b;
        cout<<(p[b]^p[a-1])<<"\n";
    }
}
