#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    long long m=LLONG_MIN,s=0;
    for(int i=0;i<n;i++)
    {
        long long x;
        cin>>x;
        s+=x;
        if(s>m) m=s;
        if(s<0) s=0;
    }
    cout<<m;
}
