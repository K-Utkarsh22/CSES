#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long n;cin>>n;
    for(auto k=1;k<=n;k++)
    {
        long long t=k*k*(k*k-1)/2;
        long long o=4*(k-1)*(k-2);
        cout<<t-o<<"\n";
    }
}
