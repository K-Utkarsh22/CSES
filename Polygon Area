#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long n;cin>>n;
    vector<long long> x(n),y(n);
    for(int i=0;i<n;i++) cin>>x[i]>>y[i];
    long long r=0;
    for(int i=0;i<n;i++)
    {
        int j=(i+1)%n;
        r+=(x[i]+x[j])*(y[i]-y[j]);
    }
    cout<<abs(r)<<"\n";
}
