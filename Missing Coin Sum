#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;cin>>n;
    vector<long long> v(n);
    for(auto &x:v) cin>>x;
    sort(v.begin(),v.end());
    long long c=1;
    for(int i=0;i<n;i++)
    {
        if(v[i]>c) break;
        c+=v[i];
    }
    cout<<c;
}
