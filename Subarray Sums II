#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    long long x,s=0,c=0;
    cin>>n>>x;
    vector<int>v(n);
    map<long long,int>m;
    m[0]=1;
    for(auto &x:v) cin>>x;
    for(auto &k:v)
    {
        s+=k;
        c+=m[s-x];
        m[s]++;
    }
    cout<<c;
}
