#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;cin>>n;
    vector<long long> v(n);
    long long sum=0;
    for(auto &x:v)
    {
        cin>>x;
        sum+=x;
    }
    sort(v.begin(),v.end());
    long long m=v[n-1];
    cout<<max(sum,2*m);
}
