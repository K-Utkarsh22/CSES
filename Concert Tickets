#include <bits/stdc++.h>
using namespace std;
long long M=1e9+7;
int main()
{
    int n,m;cin>>n>>m;
    long long x;
    multiset<long long> s;
    for(int i=0;i<n;i++) 
    {
        cin>>x;
        s.insert(x);
    }
    while(m--)
    {
        cin>>x;
        auto it=s.upper_bound(x);
        if(it==s.begin()) cout<<"-1\n";
        else
        {
            cout<<*(--it)<<"\n";
            s.erase(it);
        }
    }
}
